[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cutmix-jax"
version = "0.1.0"
description = "A lightweight JAX implementation of CutMix"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["cutmix_jax*"]
